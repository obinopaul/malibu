Use `shell_stop` to interrupt a running command or terminate the whole shell session.

- Default behavior sends an interrupt to the current command
- Set `kill_session=true` to shut down the session entirely
