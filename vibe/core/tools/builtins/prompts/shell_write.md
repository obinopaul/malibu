Use `shell_write` to send text input to an existing persistent shell session.

- This is for interactive programs that are already running
- Set `press_enter=false` if you need to send partial input without submitting it yet
