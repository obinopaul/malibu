# memory

Evaluates that AGENTS.md memory files loaded via the `memory` config are injected into the agent's system prompt and influence behavior — recall, guided actions, multiple sources, graceful handling of missing files.
