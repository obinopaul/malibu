Lists files and directories in a given path. The path parameter can be absolute, relative to the current working directory, or relative to the workspace root; omit it to use the current working directory. If a relative path does not exist under the current working directory, the tool also tries the workspace root. You can optionally provide an array of glob patterns to ignore with the ignore parameter. You should generally prefer the Glob and Grep tools if you know which directories to search.
