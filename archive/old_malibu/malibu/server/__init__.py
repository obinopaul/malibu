"""ACP server layer — MalibuAgent + supporting modules.

Implements the full Agent interface (all 15 protocol methods) with
database-backed sessions, LangGraph streaming, and permission handling.
"""
